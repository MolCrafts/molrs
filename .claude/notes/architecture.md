> Run `/mol:map` to populate this blueprint.

Project blueprint (modules, public surface, style, layer roles) for the
`librarian` agent during `/mol:spec` Step 4.5. Hand-maintained rules live in
`architecture-rules.md`; this file is the map generated/refreshed by `/mol:map`.
